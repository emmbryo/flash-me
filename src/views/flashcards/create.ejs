<div>
  <h1>
    Create a flashcard, and start learning!
  </h1>
  <div class="search-word">
    <form class="search-form" action="./flashcards/search" method="post">
      <div class="input-search">

        <div class="input-element">
          <label for="search">Enter a dutch word:</label>
          <input type="text" id="search-input" name="search" maxlength="35">
        </div>
        
        <button type="submit">Search</button>
      </div>
    </form>
  </div>
  <% if (viewData) { %>
  <div>
    <form class="create-flashcard" method="post" action="./flashcards/create">
      <div class="input-create">

        <div class="input-element">
          <label for="word">Word:</label>
          <input type="text" name="word" class="word" maxlength="100" value="<%= viewData.word %>" required>
        </div>
        

        <div class="input-element">
          <label for="translation">Translation:</label>
          <input type="text" name="translation" class="translation" maxlength="100" placeholder="<%= viewData.translation %>" required>
        </div>
        

        <div class="input-element">
          <label for="gap-sentence">Gap sentence:</label>
          <textarea name="gap-sentence" class="gap-sentence" maxlength="400" rows="5"><%= viewData.gapSentence %></textarea>
        </div>
        

        <div class="input-element">
          <label for="pronunciation">Pronunciation:</label>
          <input type="text" name="pronunciation" class="pronunciation" maxlength="100" value="<%= viewData.pronunciation %>">
        </div>
        
        <div class="input-element">
          <label for="sentence">Full sentence:</label>
          <textarea type="text" name="sentence" iclass="sentence" maxlength="400" rows="5"><%= viewData.sentence %></textarea>
        </div>
        
        <button type="submit">Save</button>
      </div>
    </form>
    <div class="warning-text">
      <h3>
        Be careful! The automatic generation is not flawless:
      </h3>
        <ul>
          <li>It does not take the dutch spelling rules into account when creating the gap sentences, hence the word might not be extracted (eg. huis --> huizen, lopen --> hij loopt)</li>
          <li>The sentences given is not always correctly formatted in terms of periods, commas and capital letters (they are directly imported from the dutch dictionary <a href="www.woorden.org">woorden.org</a>)</li>
        </ul>
        <h3>
          Please be sure to manually edit the card before saving!
        </h3>
    </div>
  </div>
  <% } %>
</div>