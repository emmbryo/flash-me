<div class="flashcards">
  <h1 id="headline">
    Get ready to flash:
  </h1>
  <% if (viewData?.length) { %>
    <div class="flashcard-container">
    <% viewData.forEach( flashcard => { %>
        <div class="flashcard">
          <div class="edit-delete-buttons">
            <button class="edit-flashcards" id="<%= flashcard.back?.word %>-edit">Edit</button>
            <button class="delete-flashcards" id="<%= flashcard.back?.word %>-delete">Delete</button>
          </div>
          <div class="front">
            <h3><b> <%= flashcard.front?.translation %> </b></h3>
            <p> <%= flashcard.front?.gapSentence %></p>
          </div>
          <div class="back hidden">
            <h3><b> <%= flashcard.back?.word %> </b></h3>
            <p><%= flashcard.back?.pronunciation %></p>
            <p><%= flashcard.back?.sentence %></p>
          </div>
          <div>
            <button class="flip-flashcards" id="<%= flashcard.back?.word %>">Flash!</button>
          </div>
        </div>
    <% }) %>
    </div>
  <% } else { %>
    <div>
      <p>No cards in the deck!</p>
    </div>
    <% } %>
</div>

<script src="./js/cards.js"></script>